import { MenuItem } from "../../models/Menu/MenuItem";

export const resumeMenuItem = new MenuItem();
export const pauseMenuItem = new MenuItem();
export const restartMenuItem = new MenuItem();
export const previousTipMenuItem = new MenuItem();
export const nextTipMenuItem = new MenuItem();
export const backMenuItem = new MenuItem();
